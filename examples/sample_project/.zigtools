# Zig Tooling Configuration
# This file configures the behavior of zig-tooling for this project

# Log file location (relative to project root)
log_path = "logs/tooling.log"

# Memory checker settings
memory_checker:
  exclude_patterns:
    - "vendor/*"
    - "build/*"
  
# Testing compliance settings  
testing_compliance:
  test_file_pattern: "test_*.zig"
  require_colocation: true